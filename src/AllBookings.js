/*array of booking objects
should be able to add bookings to itself
should be able to remove future bookings*/
class AllBookings {
  constructor(bookings) {
    this.bookings = bookings
  }

}

export default AllBookings
